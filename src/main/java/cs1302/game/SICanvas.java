/*import javafx.scene.*;
import javafx.scene.canvas.*;
import javafx.scene.paint.*;

public class SICancas extends canvas {

    Canvas canvas;
    GraphicsContext gc;

    public SICanvas() {
        canvas = new Canvas(640, 840);
        gc = canvas.getGraphicsContext2D();
        gc.setFill(Color.BLACK);
    }

    public void drawAlien(Image al, double x, double y) {
        gc.drawImage(al, x, y);
    }


}
*/
